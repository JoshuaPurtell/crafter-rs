# parity

## Purpose
Provide parity tests against the original Python Crafter reference.

## Requirements
- Maintain the same test coverage and assertions as `src/parity.rs`.
- Keep reference data (actions, achievements, materials) in sync with Python Crafter.
